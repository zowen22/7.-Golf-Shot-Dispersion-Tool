# Session Log

-----

## 2026-06-15 - Project Init + Phase 0–1 Scaffold

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Populated all 5 PM files from `GolfApp_MVP_Spec.docx` (v1.0)
- Created full Swift project folder structure and source files (Xcode-ready)
- Implemented all data models (Codable): User, GolfProfile, Club, Bag, Course, Hole, Round, DispersionModels
- Implemented AppState (ObservableObject) with auth state machine
- Implemented all service protocols and stub implementations: AuthService, FirestoreService, NetworkService, LocationService
- Implemented DispersionEngine (pure functions, fully unit testable): Tier 1 formula, Tier 3 interpolation, skew, ellipse builder
- Implemented HandicapCalculator and BearingCalculator utilities
- Implemented all ViewModels: Auth, Onboarding, Map, Bag, CourseSearch, Home, Profile
- Implemented all Views: Auth (3), Onboarding (12), Map, Course, Bag, Home, Profile, Shared
- Implemented unit tests for DispersionEngine and BagViewModel
- Configured .gitignore for Xcode
- Populated Work Packages with task ownership (@claude / @user)

### Decisions Made
- Firestore offline persistence used as local cache instead of CoreData (avoids complexity for MVP)
- RevenueCat flagged as recommended but not mandatory — left as @user decision
- Post-MVP data fields added to models now per spec's future-proofing note

### Blockers / Issues
- All @user tasks in Phase 0 required before any code can be compiled/tested in Xcode:
  - Create Xcode project and add SPM dependencies
  - Firebase dev + prod project creation + GoogleService-Info.plist
  - Golfbert API key
  - Mapbox API key

### Next Session
- Wire Firebase credentials and test auth flows in Xcode
- Validate Mapbox rendering with real API token
- Validate Golfbert API responses against Codable models
- Run DispersionEngine unit tests

-----

## 2026-06-12 - Framework: Audits + Branched Plans Support

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Added "Optional PM Artifacts" section to CLAUDE.md: Audits and Branched Plans conventions with lifecycle, naming, required fields, and rules
- Updated Session Start Routine (step 2) to check for open Audits/ and Plans/ sub-folders
- Added breadcrumb pattern: live "Open Audits / Open Plans" section in CLAUDE.md updated as artifacts open/close
- Validated in BetterGolfLeagueTracker project (first real use of Audits/ artifact type)

### Decisions Made
- Breadcrumb in CLAUDE.md (not in PM files) — ensures Claude loads it on every session without having to read all PM files first
- Branched Plans archived (not deleted) when resolved so decision history is preserved

### Blockers / Issues
- None

### Next Session
- N/A — framework update is complete; future sessions use this in real projects

-----
